---
# ---------------------------------------------------------
# Apps 角色主任务入口
# 需要常规安装到用户空间 (/data) 而非系统分区的应用关键字列表
# ---------------------------------------------------------

- import_tasks: push_system_app.yml

- import_tasks: battery.yml

- import_tasks: screensaver.yml

- import_tasks: launcher.yml

- import_tasks: virtual_soft_keys.yml

- import_tasks: push_ovpn.yml

# 最终状态检查：验证所有关键部署结果
- import_tasks: status.yml
